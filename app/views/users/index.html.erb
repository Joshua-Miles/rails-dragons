<h1>Users</h1>

	<ul><% @users.each do |user| %>
		<li><%= link_to user.name, user %>
		<%= link_to "Edit", edit_user_path(user) %></li>
		<% end %>
	</ul>

<%= link_to "Add a new User", new_user_path %>