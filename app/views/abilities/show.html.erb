<h1>Ability</h1>
<p>
  <strong>Name:</strong>
  <%= @ability.name %>
</p>

<p>
  <strong>Dragons with this ability:</strong>
  <% if @ability.dragons != [] %>
    <% @ability.dragons.each do |dragon| %>
    <li><%= link_to dragon.name, dragon_path(dragon) %></li>
    <% end %>
  <% else %>
    No dragons have this ability.
  <% end %>
</p>

<%= link_to 'Edit', edit_ability_path(@ability) %> |
<%= link_to 'Back', abilities_path %>
