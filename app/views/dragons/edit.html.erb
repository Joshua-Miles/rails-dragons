<%= link_to "< Return to Dragon", @dragon %>

<%= form_with model: @Dragon do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label "Owner:" %><br>
  <%= f.select :user_id, @all_users %>

  <%= f.submit %>

  <br>

<% end %>
